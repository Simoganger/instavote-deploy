apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: worker-dev
  namespace: instavote
spec:
  eventSeverity: info
  eventSources:
  - kind: Kustomization
    name: worker-dev
  providerRef:
    name: github-instavote